
AIBD Laboratorium 2

Files structure:

```
AiBD laboratorium 2
├── AnalysisData
│   ├── tb_but_tidy.csv  // Tuberculosis dataset but tidied up.
│   ├── tb_men_age_distribution.csv	      // Tuberculosis cases amongst men by age.
│   └── tb_women_age_distribution.csv      // Tuberculosis cases amongst women by age.
├── CommandFiles
│   └── Lab2_processing_appendix.ipynb		      // Script written in Jupyter Notebook.
├── Documents
│   └── DataAppendix.txt	      // Dataset explanation.
│   └── FinalPaper.ipynb       // Copy of the script.
└── OriginalData
    ├── Metadata
    │   └── MetadataGuide.txt	      // Info regarding the original datafile. 
    └── tb.csv	     	      // Original data.
```

Modules' versions used:

```
python >= 3.8.6
numpy >= 1.19.2
pandas >= 1.2.4
jupyter >= 1.0.0
matplotlib >= 3.3.2
```
